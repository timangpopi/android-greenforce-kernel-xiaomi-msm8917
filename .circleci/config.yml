version: 2.1
jobs:
  compile:
    docker:
      - image: ubuntu:19.10
    steps:
      - run: apt-get -y update && apt-get -y upgrade && apt-get -y install bc binutils g++ build-essential zip curl libstdc++6 git wget
      - run: git clone https://github.com/fadlyas07/android-kernel-xiaomi-msm8917-1.git -b test --depth=1 .
      - run: wget https://raw.githubusercontent.com/fadlyas07/script-circle/master/circleci-script
      - run: chmod +x circleci-script
      - run: bash circleci-script
workflows:
  version: 2.1
  cooking:
    jobs:
      - compile
